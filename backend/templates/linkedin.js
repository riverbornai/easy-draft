/**
 * templates/linkedin.js — STUB (Phase 5)
 * Full implementation: hook + 3 paragraphs + hashtags format
 */
export function applyLinkedinTemplate(_draft, _brief) {
  // TODO: Phase 5 — format draft into: hook + 3 paragraphs + hashtags
  return _draft;
}
