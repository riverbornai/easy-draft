/**
 * templates/blog.js — STUB (Phase 5)
 * Full implementation: H1 + sections + SEO meta structure
 */
export function applyBlogTemplate(_draft, _brief) {
  // TODO: Phase 5 — format draft into: H1 + sections + SEO meta
  return _draft;
}
