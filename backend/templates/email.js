/**
 * templates/email.js — STUB (Phase 5)
 * Full implementation: subject line + body + CTA format
 */
export function applyEmailTemplate(_draft, _brief) {
  // TODO: Phase 5 — format draft into: subject line + body + CTA
  return _draft;
}
