/**
 * tools/formatPost.js — STUB (Phase 5)
 * Full implementation: applies channel-specific templates to approved draft
 * Channels: linkedin | blog | xthread | email
 */
export async function formatPost(_channel, _draft) {
  // TODO: Phase 5 — apply channel template (linkedin/blog/xthread/email)
  console.log('  [formatPost] Phase 5 — coming soon.');
  return _draft;
}
