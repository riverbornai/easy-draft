# Gitleaks configuration file
title = "EasyDraft Secret Scan Rules"

[allowlist]
description = "Allowlist for harmless example files"
paths = [
  '''\.env\.example$''',
  '''package-lock\.json$''',
  '''yarn\.lock$'''
]
